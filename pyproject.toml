[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "superHeli"
version = "0.1.0"

[tool.pytest.ini_options]
minversion = "6.0"
testpaths = ["tests"]
